// describes the 'mode' that the prompt component is currently in
export enum Modes {
    request = 'request',   // used when forming a request
    response = 'response', // used when waiting for a response
  }
  